(function(){'use strict';
const app=document.getElementById('vatReconciliationApp');if(!app)return;const api=app.dataset.api,content=document.getElementById('vrContent'),period=document.getElementById('vrPeriod'),notice=document.getElementById('vrNotice');let state=null;
const esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const money=v=>'N$ '+Number(v||0).toLocaleString('en-NA',{minimumFractionDigits:2,maximumFractionDigits:2});const badge=(text,kind)=>`<span class="vr-badge ${kind||''}">${esc(text)}</span>`;
function options(code){const year=Number(code.slice(0,4));let out='';for(let y=year-2;y<=year+1;y++)for(let m=1;m<=11;m+=2){const k=y+'-'+String(m).padStart(2,'0'),end=new Date(y,m+1,0);const label=new Date(y,m-1,1).toLocaleString('en',{month:'short'})+'–'+end.toLocaleString('en',{month:'short',year:'numeric'});out+=`<option value="${k}" ${k===code?'selected':''}>${label}</option>`;}return out;}
function sourceCard(label,value,ready,detail,href){return`<article class="vr-source-card"><header><h3>${esc(label)}</h3>${badge(ready?'Complete':'Provisional',ready?'is-ready':'is-pending')}</header><strong>${money(value)}</strong><p>${esc(detail)}</p><a href="${href}">Open source →</a></article>`;}
function render(d){state=d;period.innerHTML=options(d.period.period_code);document.getElementById('vrDue').textContent='Due '+d.period.due_date;const s=d.sources,snap=d.snapshot,position=snap?Number(snap.final_position):Number(s.final_position);const status=d.source_changed?'Historical change detected':(d.status==='ready_to_file'?'Ready to file':d.status==='filed'?'Filed':d.ready?'Review complete':'Provisional');const imports=s.imports.map(r=>`<tr><td>${esc(r.import_date)}<small>${esc(r.reference)}</small></td><td>${money(r.import_vat_amount)}<small>Paid ${money(r.paid)}</small></td><td>${badge(r.evidence_status,r.evidence_status==='evidence_present'?'is-ready':'is-pending')}</td><td><form class="vr-inline-form" data-action="eligibility"><input type="hidden" name="import_id" value="${r.id}"><select name="eligibility_status"><option value="pending_review" ${r.eligibility_status==='pending_review'?'selected':''}>Pending review</option><option value="eligible" ${r.eligibility_status==='eligible'?'selected':''}>Eligible</option><option value="excluded" ${r.eligibility_status==='excluded'?'selected':''}>Excluded</option><option value="needs_evidence" ${r.eligibility_status==='needs_evidence'?'selected':''}>Needs evidence</option></select><input name="eligible_amount" type="number" step="0.01" value="${esc(r.eligible_amount||r.import_vat_amount)}" aria-label="Eligible amount"><input name="reason" value="${esc(r.decision_reason||'')}" placeholder="Decision reason"><button>Save</button></form></td></tr>`).join('')||'<tr><td colspan="4">No Import VAT records are available for review.</td></tr>';
content.innerHTML=`<section class="vr-status-line"><div><p class="eyebrow">${esc(d.period.label)}</p><h2>${esc(status)}</h2><p>${d.source_changed?'The frozen return differs from current saved source data. Review before further action.':'Figures are read from saved local VAT applications only.'}</p></div>${badge(status,d.source_changed?'is-alert':d.ready?'is-ready':'is-pending')}</section><section class="vr-summary"><article><span>Final Output VAT</span><strong>${money(snap?snap.output_vat:s.output_vat)}</strong></article><article><span>Local Input VAT</span><strong>${money(snap?snap.input_vat:s.input_vat)}</strong></article><article><span>Eligible Import VAT</span><strong>${money(snap?snap.import_vat:s.import_vat)}</strong></article><article class="is-position"><span>${position>=0?'VAT Payable':'VAT Credit'}</span><strong>${money(Math.abs(position))}</strong></article></section><section class="vr-source-grid">${sourceCard('Output VAT',s.output_vat,s.output_ready,'Completed monthly final Output VAT','output-vat.php')}${sourceCard('Input VAT',s.input_vat,s.input_ready,'Saved local purchases in completed months','input-vat.php')}${sourceCard('Import VAT',s.import_vat,s.import_ready,s.pending_imports+' record(s) pending eligibility review','import-vat.php')}</section><section class="vr-panel"><header><div><p class="eyebrow">Show the working</p><h2>Final VAT calculation</h2></div></header><div class="vr-equation"><span>${money(s.output_vat)}<small>Output VAT</small></span><b>−</b><span>${money(s.input_vat)}<small>Input VAT</small></span><b>−</b><span>${money(s.import_vat)}<small>Eligible Import VAT</small></span><b>+</b><span>${money(s.adjustments)}<small>Adjustments</small></span><b>=</b><span>${money(s.final_position)}<small>Current position</small></span></div></section><section class="vr-panel"><header><div><p class="eyebrow">Owner review</p><h2>Import VAT credit eligibility</h2></div><span>Assessed and paid do not automatically mean claimable.</span></header><div class="accounts-table-wrap"><table><thead><tr><th>Import</th><th>Assessed / Paid</th><th>Evidence</th><th>Owner decision</th></tr></thead><tbody>${imports}</tbody></table></div></section>${actions(d)}<p class="vr-audit-note">No source transaction is changed. Penalties and interest remain excluded from Import VAT credit.</p>`;}
function actions(d){if(d.status==='filed')return`<section class="vr-panel"><h2>Return filed</h2><p>${esc(d.filing.filed_date)} · ${esc(d.filing.filing_reference)}</p><p>Paid ${money(d.paid)} · Balance ${money(d.balance)}</p><form class="vr-action-form" data-action="payment"><input type="date" name="payment_date" required><input type="number" step="0.01" name="amount" placeholder="Payment amount" required><input name="reference" placeholder="Payment reference"><button>Record payment</button></form></section>`;if(d.snapshot)return`<section class="vr-panel"><h2>Frozen snapshot · Ready to file</h2><p>Source hash ${esc(d.snapshot.source_hash.slice(0,12))}…</p><form class="vr-action-form" data-action="file"><input type="date" name="filed_date" required><input name="reference" placeholder="NamRA filing reference" required><button>Record filing</button></form></section>`;return`<section class="vr-panel"><h2>Return controls</h2><p>${d.ready?'All three source reviews are complete. Freeze an immutable snapshot when ready.':'Complete each source and Import VAT eligibility review first.'}</p><form class="vr-action-form" data-action="adjustment"><select name="direction"><option value="increase">Increase payable</option><option value="decrease">Decrease payable</option></select><input type="number" step="0.01" name="amount" placeholder="Adjustment amount"><input name="reason" placeholder="Adjustment reason"><button>Record adjustment</button></form><button class="accounts-button" data-ready ${d.ready?'':'disabled'}>Mark Ready to File</button></section>`;}
async function load(){const res=await fetch(api+'?period='+encodeURIComponent(period.value||new Date().toISOString().slice(0,7)),{credentials:'same-origin',cache:'no-store'}),json=await res.json();if(!json.ok)throw new Error(json.message);render(json.data);}async function submit(form,action){const fd=new FormData(form);fd.set('csrf',state.csrf);fd.set('period',state.period.period_code);fd.set('action',action);const res=await fetch(api,{method:'POST',body:fd,credentials:'same-origin'}),json=await res.json();if(!json.ok)throw new Error(json.message);notice.hidden=false;notice.textContent=json.message;render(json.data);}
app.addEventListener('submit',e=>{const f=e.target.closest('form[data-action]');if(!f)return;e.preventDefault();submit(f,f.dataset.action).catch(err=>{notice.hidden=false;notice.textContent=err.message;});});app.addEventListener('click',e=>{const step=e.target.closest('[data-period-step]');if(step){const d=new Date((period.value||state.period.period_code)+'-01T12:00:00');d.setMonth(d.getMonth()+Number(step.dataset.periodStep));period.value=d.toISOString().slice(0,7);load();}if(e.target.closest('#vrRefresh'))load();if(e.target.closest('[data-ready]'))submit(document.createElement('form'),'ready').catch(err=>{notice.hidden=false;notice.textContent=err.message;});});period.addEventListener('change',load);load().catch(err=>{content.innerHTML='<p class="accounts-empty">'+esc(err.message)+'</p>';});
})();
