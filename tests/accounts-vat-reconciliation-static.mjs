import fs from 'node:fs';import assert from 'node:assert/strict';
const shared=fs.readFileSync('shared/accounts-vat-reconciliation.php','utf8');const api=fs.readFileSync('apps/accounts/vat-reconciliation-api.php','utf8');const page=fs.readFileSync('apps/accounts/vat-reconciliation.php','utf8');const js=fs.readFileSync('assets/js/vat-reconciliation.js','utf8');const accounts=fs.readFileSync('apps/accounts/index.php','utf8');
assert.match(shared,/vat_reconciliation_require_owner/);assert.match(shared,/accounts_import_vat_credit_eligibility/);assert.match(shared,/accounts_vat_reconciliation_snapshots/);assert.match(shared,/output-\$input-\$eligible\+\$adjustments/);assert.match(api,/pending_review.*eligible.*excluded.*needs_evidence/);assert.match(api,/snapshot_frozen/);assert.match(page,/VAT Reconciliation/);assert.match(accounts,/vat-reconciliation\.php/);assert.match(js,/Assessed and paid do not automatically mean claimable/);assert.ok(!/\bmatch\s*\(/.test(shared),'PHP 8 match expression is not production-compatible');
const output=47241.30,input=9247.40,imports=13529.69;assert.equal(Math.round((output-input-imports)*100)/100,24464.21);
console.log('VAT Reconciliation static contracts and authoritative formula passed.');
