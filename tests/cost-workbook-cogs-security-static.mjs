import fs from 'node:fs';
const root=new URL('../',import.meta.url);const api=fs.readFileSync(new URL('apps/cost-manager/cw-cogs-api.php',root),'utf8');const adapter=fs.readFileSync(new URL('shared/cost-workbook-cogs.php',root),'utf8');const page=fs.readFileSync(new URL('apps/cost-manager/landing-cost-engine.php',root),'utf8');
const ok=(v,m)=>{if(!v)throw new Error(m)};
ok(/require_role\('owner_admin'\)/.test(api),'Endpoint must be Owner-only.');
ok(/if\(\$method==='POST'\)cw_require_csrf\(\)/.test(api),'POST must require nonce.');
ok(/in_array\(\$method,\['GET','POST'\]/.test(api),'Request method gate missing.');
ok(/count\(\$ids\)!==1/.test(api),'Single-line atomic boundary missing.');
ok(/v\.status='confirmed'/.test(api),'Confirmed immutable version requirement missing.');
ok(/\$pdo->prepare\(\$sql\)/.test(api),'Prepared source query missing.');
ok(/woocommerce_cogs_disabled/.test(adapter)&&/feature_enabled/.test(api),'Server feature gate missing.');
ok(/woocommerce_cogs_stale/.test(adapter),'Stale-value rejection missing.');
ok(/woocommerce_cogs_verification_failed/.test(adapter),'Returned-value verification missing.');
ok(!/WC_CONSUMER_(?:KEY|SECRET)|consumer_secret|consumer_key/.test(api+adapter),'Credential reference leaked into Phase 3 boundary.');
ok(!/meta_data|_cogs_value|\bUPDATE\s+wp_|\bINSERT\s+INTO\s+wp_/i.test(api+adapter),'Direct WooCommerce metadata/database write found.');
ok(!/retry|for\s*\([^)]*attempt/i.test(adapter),'Mutation retry found.');
ok(/\$safe=CostWorkbookNativeCogs::safeError\(\$e\);cw3_json\(\$safe/.test(api),'Raw remote errors are not normalized.');
ok(/Cache-Control: no-store, private/.test(api),'Financial response cache control missing.');
ok(/require_role\('owner_admin'\)/.test(page),'Profitability page must not expose financial fields to employees.');
ok(/WooCommerce Cost of Goods Sold is not enabled\. Cost publishing is unavailable\./.test(page),'Disabled-feature notice missing.');
console.log('Cost Workbook Phase 3 endpoint security checks passed.');
